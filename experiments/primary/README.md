# Step 15 Primary Experiments v1.0.0

Collection is complete: all 132 frozen safety-disabled run cells have accepted attempts. The campaign is paused and complete, with no active or pending run.

The `results` directory stores one directory per run ID and one immutable subdirectory per attempt. Failed attempts are retained and accepted attempts are indexed in `reports/completed-runs.csv`. Never overwrite an attempt.

See `reports/STEP-15-PRIMARY-EXPERIMENTAL-DATASET-REPORT.md` and `validation/final-campaign-audit.json` for the final completion record.
