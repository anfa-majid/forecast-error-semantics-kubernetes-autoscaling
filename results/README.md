# Results

`reference/statistical/` contains the sealed Step 18 tables and six paper
figures. `reference/robustness/` contains the prospective sensitivity analysis.
`reproduced/` is intentionally ignored except for its placeholder and receives
locally regenerated outputs.

Run `scripts/reproduce-figures.ps1` to recreate and byte-compare the 12 core
statistical tables/figures.
