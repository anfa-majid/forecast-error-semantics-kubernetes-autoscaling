apiVersion: v1
kind: Service
metadata:
  name: benchmark-app
  labels: {app.kubernetes.io/name: benchmark-app, anfa.dev/component: benchmark}
spec:
  type: NodePort
  selector: {app.kubernetes.io/name: benchmark-app}
  ports:
    - {name: http, port: 8080, targetPort: http, nodePort: 30080, protocol: TCP}
